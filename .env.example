# LLM 后端（OpenAI 兼容，createOpenAiModelProvider 读取这些变量）
OPENAI_API_KEY=sk-xxxx
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o

# 工作台本地目录
# 会话/历史持久化目录（JSONL）
ZMZAI_DATA_DIR=./.harness-data
# Agent 运行时的工作区根目录（直接读写本机文件/跑命令的地方）
ZMZAI_WORKSPACE=./.workspace
